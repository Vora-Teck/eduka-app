<!-- ==================== ATTENDANCE PAGE ==================== -->
<section id="attendance-page" class="section hidden bg-gray-50 dark:bg-gray-950 min-h-screen pb-24">

    <!-- Header -->
    <div class="px-6 pt-8 pb-4 bg-white dark:bg-gray-900 border-b border-gray-100 dark:border-gray-800 flex items-center justify-between">
        <h1 class="text-3xl font-bold tracking-tight">Attendance</h1>
        <div onclick="toggleDarkMode()" 
             class="w-9 h-9 bg-gray-100 dark:bg-gray-800 rounded-2xl flex items-center justify-center cursor-pointer active:scale-95 transition">
            <i id="attendance-theme-icon" class="fa-solid fa-moon text-lg"></i>
        </div>
    </div>

    <div class="px-6 pt-6 space-y-8">

        <!-- Overall Metrics -->
        <div class="bg-white dark:bg-gray-900 rounded-3xl p-6">
            <div class="flex items-center justify-between mb-6">
                <div>
                    <div class="text-sm font-medium text-gray-500">TERM 2 • 2025/2026</div>
                    <h2 class="text-2xl font-semibold">Overall Attendance</h2>
                </div>
                <!-- Progress Ring -->
                <div class="relative">
                    <svg class="w-28 h-28 -rotate-12" viewBox="0 0 42 42">
                        <circle cx="21" cy="21" r="15" fill="none" stroke="#e5e7eb" stroke-width="6"></circle>
                        <circle cx="21" cy="21" r="15" fill="none" stroke="#10b981" 
                                stroke-width="6" stroke-dasharray="94.2" 
                                stroke-dashoffset="7.536" stroke-linecap="round"></circle>
                    </svg>
                    <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 text-center">
                        <div class="text-5xl font-bold text-emerald-600">92%</div>
                    </div>
                </div>
            </div>

            <!-- Stats -->
            <div class="grid grid-cols-3 gap-4 text-center">
                <div>
                    <div class="text-3xl font-semibold text-emerald-600">28</div>
                    <div class="text-xs text-gray-500">Days Present</div>
                </div>
                <div>
                    <div class="text-3xl font-semibold text-red-500">3</div>
                    <div class="text-xs text-gray-500">Days Absent</div>
                </div>
                <div>
                    <div class="text-3xl font-semibold">31</div>
                    <div class="text-xs text-gray-500">School Days</div>
                </div>
            </div>
        </div>

        <!-- Filters -->
        <div class="flex gap-3">
            <!-- Term Filter -->
            <select id="term-filter" onchange="filterAttendance()" 
                    class="flex-1 bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-3xl px-5 py-4 text-base outline-none">
                <option value="term2">Term 2 • 2025/2026</option>
                <option value="term1">Term 1 • 2025/2026</option>
                <option value="term3">Term 3 • 2025/2026</option>
            </select>

            <!-- Week Filter -->
            <select id="week-filter" onchange="filterAttendance()" 
                    class="flex-1 bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-3xl px-5 py-4 text-base outline-none">
                <option value="all">All Weeks</option>
                <option value="week1">Week 1</option>
                <option value="week2">Week 2</option>
                <option value="week3">Week 3</option>
                <option value="week4">Week 4</option>
                <option value="week5">Week 5</option>
                <option value="week6">Week 6</option>
                <option value="week7">Week 7</option>
                <option value="week8" selected>Week 8 (Current)</option>
            </select>
        </div>

        <!-- Weekly Summary -->
        <div>
            <h3 class="text-sm font-semibold text-gray-500 mb-4 px-1">Weekly Summary</h3>
            <div id="weekly-grid" class="grid grid-cols-4 gap-3">
                <!-- Populated by JS -->
            </div>
        </div>

        <!-- Detailed Register -->
        <div>
            <div class="flex justify-between items-center mb-4 px-1">
                <h3 class="text-sm font-semibold text-gray-500">Daily Register</h3>
                <span onclick="exportAttendance()" class="text-xs text-emerald-600 flex items-center gap-1 cursor-pointer">
                    <i class="fa-solid fa-download"></i> Export PDF
                </span>
            </div>
            
            <div id="attendance-register" class="bg-white dark:bg-gray-900 rounded-3xl divide-y divide-gray-100 dark:divide-gray-700">
                <!-- Populated by JS -->
            </div>
        </div>

        <!-- Today's Attendance Quick Action -->
        <button onclick="markTodayAttendance()" 
                class="w-full bg-emerald-600 hover:bg-emerald-700 text-white font-semibold py-5 rounded-3xl flex items-center justify-center gap-3 active:scale-95 transition">
            <i class="fa-solid fa-calendar-check"></i>
            <span>Mark Today's Attendance</span>
        </button>
    </div>

    <script>
        // Demo Attendance Data
        const attendanceData = {
            term2: {
                overall: { percentage: 92, present: 28, absent: 3, total: 31 },
                weeks: {
                    week1: { percentage: 100, present: 5, absent: 0, days: [
                        { date: "06 Apr", status: "present", reason: "" },
                        { date: "07 Apr", status: "present", reason: "" },
                        { date: "08 Apr", status: "present", reason: "" },
                        { date: "09 Apr", status: "present", reason: "" },
                        { date: "10 Apr", status: "present", reason: "" }
                    ]},
                    week2: { percentage: 80, present: 4, absent: 1, days: [
                        { date: "13 Apr", status: "present", reason: "" },
                        { date: "14 Apr", status: "absent", reason: "Sick" },
                        { date: "15 Apr", status: "present", reason: "" },
                        { date: "16 Apr", status: "present", reason: "" },
                        { date: "17 Apr", status: "present", reason: "" }
                    ]},
                    week3: { percentage: 100, present: 5, absent: 0, days: [
                        { date: "20 Apr", status: "present", reason: "" }
                    ]},
                    week4: { percentage: 90, present: 4, absent: 1, days: [] },
                    week5: { percentage: 100, present: 5, absent: 0, days: [] },
                    week6: { percentage: 80, present: 4, absent: 1, days: [] },
                    week7: { percentage: 95, present: 5, absent: 0, days: [] },
                    week8: { percentage: 100, present: 4, absent: 0, days: [
                        { date: "13 Apr", status: "present", reason: "" },
                        { date: "14 Apr", status: "present", reason: "" },
                        { date: "15 Apr", status: "present", reason: "" },
                        { date: "16 Apr", status: "present", reason: "" }
                    ]}
                }
            }
        };

        let currentTerm = "term2";
        let currentWeek = "all";

        // Render Weekly Summary Grid
        function renderWeeklyGrid() {
            const weeks = attendanceData[currentTerm].weeks;
            let html = '';
            
            Object.keys(weeks).forEach(weekKey => {
                const week = weeks[weekKey];
                const isSelected = currentWeek === weekKey || currentWeek === "all";
                
                html += `
                <div onclick="selectWeek('${weekKey}')" 
                     class="bg-white dark:bg-gray-800 rounded-3xl p-4 text-center cursor-pointer active:scale-95 transition ${isSelected ? 'ring-2 ring-emerald-500' : ''}">
                    <div class="text-xs text-gray-500 mb-1">${weekKey.toUpperCase()}</div>
                    <div class="text-3xl font-bold text-emerald-600">${week.percentage}%</div>
                    <div class="text-[10px] text-gray-400">${week.present}/${week.present + week.absent}</div>
                </div>`;
            });
            
            $('#weekly-grid').html(html);
        }

        // Render Detailed Register
        function renderRegister() {
            const weeksData = attendanceData[currentTerm].weeks;
            let allDays = [];
            
            if (currentWeek === "all") {
                Object.keys(weeksData).forEach(key => {
                    if (weeksData[key].days && weeksData[key].days.length) {
                        allDays = allDays.concat(weeksData[key].days);
                    }
                });
            } else if (weeksData[currentWeek]) {
                allDays = weeksData[currentWeek].days;
            }
            
            let html = '';
            
            if (allDays.length === 0) {
                html = `<div class="p-12 text-center text-gray-400">No attendance records for this selection yet.</div>`;
            } else {
                allDays.forEach(day => {
                    const statusIcon = day.status === "present" 
                        ? `<i class="fa-solid fa-check-circle text-emerald-500 text-2xl"></i>` 
                        : `<i class="fa-solid fa-circle-xmark text-red-500 text-2xl"></i>`;
                    
                    html += `
                    <div class="px-6 py-5 flex items-center justify-between">
                        <div class="flex items-center gap-4">
                            <div class="text-base font-medium">${day.date}</div>
                            ${day.reason ? `<span class="text-xs bg-red-100 text-red-600 px-3 py-1 rounded-3xl">${day.reason}</span>` : ''}
                        </div>
                        <div class="flex items-center gap-3">
                            ${statusIcon}
                            <span class="capitalize text-sm font-medium">${day.status}</span>
                        </div>
                    </div>`;
                });
            }
            
            $('#attendance-register').html(html);
        }

        // Filter handler
        function filterAttendance() {
            currentTerm = $('#term-filter').val();
            currentWeek = $('#week-filter').val();
            renderWeeklyGrid();
            renderRegister();
        }

        // Select specific week from grid
        function selectWeek(weekKey) {
            currentWeek = weekKey;
            $('#week-filter').val(weekKey);
            renderWeeklyGrid();
            renderRegister();
        }

        // Mark Today's Attendance (demo)
        function markTodayAttendance() {
            showToast("Today's attendance marked as Present ✓", false);
            // In real app this would call API and refresh register
            setTimeout(() => {
                renderRegister();
            }, 1200);
        }

        // Export demo
        function exportAttendance() {
            showToast("Attendance register exported as PDF", false);
        }

        // Initialize Attendance Page
        $(document).ready(function() {
            renderWeeklyGrid();
            renderRegister();
            
            console.log('%c📅 Attendance Page Loaded Successfully', 'color:#10b981; font-weight:600');
        });

        // Global functions
        window.toggleDarkMode = toggleDarkMode;
        window.showToast = showToast;
        window.filterAttendance = filterAttendance;
        window.selectWeek = selectWeek;
        window.markTodayAttendance = markTodayAttendance;
        window.exportAttendance = exportAttendance;
    </script>
</section>